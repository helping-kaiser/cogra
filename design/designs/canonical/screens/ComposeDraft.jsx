/* COMPOSE OPENED WITH A DRAFT ALREADY ON THE DEVICE (legacy conversion, the
   conformance round; reworked by jakob's re-review 2026-09-15). The wizard
   keeps what was started — leaving it never asks, because nothing is lost — so
   the next opening has to say so and offer both ways: back into the draft, or
   past it.

   THE DRAFT IS A `Card`, AND IT LEADS. It is the answer to a question the
   author has not asked yet ("where did that go?"), so it sits above the fresh
   start rather than beside it, and the pick step goes on underneath at 55% —
   present, clearly not the subject, and the thing Discard hands the author
   back. What the re-review added is the MARK: the card wears the brand's
   `--ring-task` edge, so the reason the screen is held is legible before a word
   of it is read.

   THE FRESH-START LINE IS NOT `PickPrompt`. That master pairs the instruction
   with the way out of the media path, and both halves are required; here there
   is no escape to offer — the draft's own pair is the choice on this board —
   so the line is spelled at the master's caption values and nothing else.

   THE LINE ENDS ON ITS DASH (jakob's ruling, 2026-08-31, recorded 2026-09-09):
   the grid below is the rest of the sentence — how you start a new post is
   obvious from what the dash points at, and a clause spelling it out would
   repeat the fresh composer's caption.

   THE BODY IS `ComposeDraftBody`, drawn once: `ComposeDraftDiscard` is this
   board with the roll's own answer raised over it, and a stage copied per board
   is a stage that drifts. The shape it draws is WEB's, on both platforms —
   dimmed and out of reach — and the roll's shield is what a tap on the pictures
   now finds instead of nothing. */
export function Screen() {
  return <ComposeDraftBody />;
}
