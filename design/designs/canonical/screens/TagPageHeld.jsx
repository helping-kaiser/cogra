/* THE TAG PAGE WITH THE TOPIC HELD (the topic round, 2026-09-14) — the state
   an Affinity leaves the page in, drawn so neither client guesses it.

   HELD MEANS THE NETTED BUNDLE IS NOT (0, 0) (jakob's predicate). That is the
   whole test: a topic with records behind it whose sum has been walked back to
   nothing is NOT held, and reads as every walked-back stance reads.

   THE ROW IS THE ONLY THING THAT MOVES, and it does not move at all —
   `ProfileOtherHeld`'s answer, for the same reason. The anchor is one control
   at the column's width in both states; held, it carries the face the twenty
   anchors answer with and the exact pair beside it, and the pair paints only in
   geek mode. The geometry is mode-invariant like every other board's, and the
   button's accessible name carries the values in both modes.

   THE FACE TABLE IS THE STANCE'S (jakob, 2026-09-14). One gesture, one face
   table: an Affinity is not a second kind of feeling and does not get a second
   set of glyphs the way a tag's claim does. What belongs to the family is the
   four words at the field's edges, and those are not on this board — the pad is
   the master's, reached by a tap.

   POSITIVE HERE, AND THAT IS NOT THE PREDICATE. This board draws a held topic
   whose association is positive, which is also what the topic feed admits. A
   NEGATIVE Affinity is held exactly as much: it stands as a public record, it
   shows in Your topics with its own face, and its one difference is that its
   topic never narrows anyone's feed. Nothing on this page marks that
   difference, because on this page there is none.

   THE BODY IS `TagPage`'s, one prop apart — a held stance is a STATE of that
   page, not a second page. */

export const FRAME = { width: 390, height: 1344 };

export function Screen() {
  return <TagPageBody bundle={mkBundle(0.6, 0.35)} />;
}
