/* THE OPINIONS PAGE WITH THE SAME SPLIT ROWS — the person↔person half of the
   door rule (jakob's rulings 2026-09-22).

   ONE ROW SHAPE, ONE PAIR OF DESTINATIONS, WHEREVER THE ROW IS MET. This page
   and the post's opinions sheet draw the same master, so a reader who learns
   that the value opens a timeline on one of them has learned it on both. That
   is the whole reason the door lives on the master rather than on either
   surface.

   THE DIRECTION IS THE PAGE'S, AND THE TIMELINE INHERITS IT. On the tab drawn
   here the rows are people who hold an opinion ON @ada, so a row's value opens
   that person's records toward her — the sheet's title spells both ends
   (`StanceTimeline`), because a timeline with one name on it would leave the
   reader guessing which way it runs.

   EVERY ROW IS A DOOR, not just the first. A list where some values opened
   something and others did not would teach the reader to test each one. */
export const FRAME = { width: 390, height: 844 };

export function Screen() {
  return <ProfileStancesExcerpt onOpenHistory={() => {}} />;
}
