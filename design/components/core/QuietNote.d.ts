/**
 * One quiet line about the surface the reader is on — what it is, how it
 * works, or what a gesture does. `label-small` in `text-secondary`, no margin
 * of its own. Never an action, never an error.
 */
export interface QuietNoteProps {
  children: React.ReactNode;
}

export declare function QuietNote(props: QuietNoteProps): JSX.Element;
