mutation LogIn($input: LogInInput!) {
  logIn(input: $input) {
    auth {
      accessToken
      refreshToken
      user {
        ...UserFields
      }
    }
    userErrors {
      message
      code
      field
    }
  }
}
