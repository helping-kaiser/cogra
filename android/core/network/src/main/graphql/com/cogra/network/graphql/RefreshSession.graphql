mutation RefreshSession($input: RefreshSessionInput!) {
  refreshSession(input: $input) {
    auth {
      accessToken
      refreshToken
    }
    userErrors {
      message
      code
      field
    }
  }
}
