<?xml version="1.0" encoding="utf-8"?>
<!-- Cleartext is allowed only to the emulator's host-loopback addresses so the
     dev backend (make dev, plain http) is reachable. Everything else stays
     TLS-only. Tighten this when a real endpoint lands. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
