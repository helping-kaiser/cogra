# Claim matrix · `reg:registers:claim-matrix`

Generated by the corpus linter's regeneration mode, and maintained only by
regeneration: this file is compared byte for byte on every run, and a hand
edit is a finding rather than a change.

Owner pkg.l1-standin, profile rust-test: one row per claim, ordered bytewise by claim
label. Every occurrence is a citation; the tests of a row are the test
that mints the claim and every test that cites it.

| Claim                                                              | Statement                                                                                                | Tests                                                                          |
| ------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| (`claim:close:a-dependency-defers-then-precedes-its-dependent`)    | A dependency defers its dependent until it lands, then precedes it in both the order and the causal key. | (`test:integration:dependencies-defer-and-order`)                              |
| (`claim:close:an-empty-epoch-publishes-nothing`)                   | An epoch with nothing approved in it publishes no package at all.                                        | (`test:integration:empty-close-publishes-nothing`)                             |
| (`claim:close:an-unfunded-act-defers-until-its-burn-lands`)        | An author who cannot pay the θ-debit defers until a burn funds the act.                                  | (`test:integration:insolvent-authors-defer-until-funded`)                      |
| (`claim:close:the-act-budget-caps-the-epoch`)                      | An epoch closes as soon as the act budget fills, so each act lands in its own epoch.                     | (`test:integration:act-budget-caps-the-epoch`)                                 |
| (`claim:close:the-interval-loop-publishes-without-being-asked`)    | The close loop publishes an approved act on its interval, with no close asked for.                       | (`test:integration:close-loop-publishes-on-the-interval`)                      |
| (`claim:handshake:a-completed-act-closes-into-a-published-record`) | A completed handshake closes into one published record, and the ingest read returns it unchanged.        | (`test:integration:handshake-lands-a-record`)                                  |
| (`claim:handshake:only-the-actors-own-witness-approves`)           | Only the actor's own approval witness lands the act, and approving twice lands it once.                  | (`test:integration:approve-verifies-the-witness`)                              |
| (`claim:record:a-founding-participant-self-loops-at-its-mint`)     | A founding Participant lands both legs at the act's own mint, the T-leg's parameters transposed.         | (`test:integration:founding-participant-self-loops-at-its-own-mint`)           |
| (`claim:record:a-hyper-act-projects-two-legs-at-one-key`)          | A hyper act projects two legs at one causal key and consummates exactly one θ-debit.                     | (`test:integration:hyper-acts-project-two-legs-at-one-key`)                    |
| (`claim:record:a-publish-toward-an-existing-mint-revises`)         | A Publish toward an existing Content node lands as a revision rather than a fresh mint.                  | (`test:integration:publish-toward-an-existing-mint-revises-rather-than-mints`) |
| (`claim:record:maturity-reads-the-degree-before-the-act`)          | Maturity reads the degree the endpoints carried before the act, and no later act revises it.             | (`test:integration:maturity-grows-with-prior-degree`)                          |
| (`claim:seal:authentication-failures-reject-at-seal`)              | A proposal whose signature does not bind its author, body, and payload rejects at seal.                  | (`test:integration:seal-rejects-authentication-failures`)                      |
| (`claim:seal:bid-mints-fresh-where-send-may-revisit`)              | A Bid may only mint a fresh Offer, where an ordinary Send toward an existing Message stays legal.        | (`test:integration:bid-is-fresh-mint-only-while-ordinary-send-stays-legal`)    |
| (`claim:seal:formation-failures-leave-no-record-behind`)           | A proposal that fails formation rejects at seal and leaves no Layer 1 object behind.                     | (`test:integration:seal-rejects-formation-failures`)                           |
| (`claim:seal:identifier-reuse-is-a-conflict`)                      | Reusing an act identifier or an author-local sequence number rejects as a conflict.                      | (`test:integration:seal-rejects-identifier-reuse-and-key-change`)              |
